package com.bootstrap.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FlywayConfig {
    // Spring Boot will automatically configure Flyway with the application.properties settings
    // No need to manually configure Flyway bean
}
